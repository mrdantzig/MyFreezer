# Secrets — never commit
.env
.env.*
!.env.example
*.env
!*.env.example

# Dependencies
node_modules/

# Local app data (contains password hashes / user data)
data/

# Vercel CLI project link/local build cache
.vercel/

.vercel
.env*
