# PRD — 2단계: 재료 기반 레시피 생성

## 개요
1단계에서 확정된 식재료 목록을 입력으로 받아, OpenRouter의 텍스트 모델을 이용해 레시피를 생성하고 사용자에게 추천한다.

## 목표
- 보유 재료로 실제로 만들 수 있는 레시피를 사용자가 이해하기 쉬운 형태(제목, 재료, 조리 단계)로 제공한다.
- 여러 후보 레시피 중 사용자가 선택할 수 있게 한다.

## 범위
**포함**
- 재료 목록 → 레시피 생성 API 호출
- 레시피 후보 목록 표시 및 상세 보기
- 선택적 사용자 조건(매운맛/채식/조리시간 등) 반영

**제외 (다른 단계 담당)**
- 이미지 인식 (1단계)
- 레시피 저장, 사용자 프로필 (3단계)

## 사용자 플로우
1. 1단계에서 확정된 재료 목록을 이어받는다.
2. (선택) 사용자가 추가 조건을 지정한다 — 예: 채식, 매운맛 선호, 조리 시간 30분 이내.
3. "레시피 추천받기" 클릭 → 로딩 상태.
4. API 응답으로 2~3개의 레시피 후보를 카드 형태로 표시 (제목, 예상 조리시간, 난이도, 필요 재료 요약).
5. 카드를 클릭하면 상세 화면에서 전체 재료·조리 단계를 확인.
6. "저장하기" 버튼으로 3단계로 전달 (3단계 기능이 있을 때만 노출).

## API 연동 사양
| 항목 | 값 |
|---|---|
| Endpoint | `POST https://openrouter.ai/api/v1/chat/completions` |
| Model | `cohere/north-mini-code:free` |
| 인증 | `Authorization: Bearer $OPENROUTER_API_KEY` (`.env`에서 로드) |
| 입력 형식 | `messages`에 1단계 재료 목록 + 사용자 조건(선택) + "레시피를 JSON 배열로 답하라"는 지시 텍스트 |

### 프롬프트 설계 요구사항
- 재료 목록을 명시적으로 나열하고, "주어진 재료만 사용하거나 최소한만 추가 재료를 제안"하도록 지시.
- 출력 형식을 JSON 스키마로 강제 지시 (아래 데이터 모델 참고), few-shot 예시 포함.
- 사용자가 지정한 조건(채식/알레르기/시간 제한 등)을 프롬프트에 포함해 반영.
- 형식 이탈 시 1회 재시도, 이후 실패 시 원문 텍스트를 그대로 보여주는 폴백 처리.

## 데이터 모델
```
Recipe {
  title: string
  ingredients: string[]      // 필요 재료 (보유 재료 + 모델이 제안한 추가 재료 구분 가능)
  missingIngredients?: string[]  // 사용자가 갖고 있지 않은 추가 필요 재료
  steps: string[]             // 조리 단계 (순서대로)
  cookTimeMinutes?: number
  difficulty?: "easy" | "medium" | "hard"
}
```

## 기능 요구사항
1. 1단계에서 전달된 `Ingredient[]`를 프롬프트에 반영해 API 호출.
2. 선택적 필터 UI 제공: 식단 제한, 매운맛 정도, 조리 시간.
3. 응답을 파싱해 레시피 카드 리스트로 렌더링 (최소 1개, 목표 2~3개).
4. 부족한 재료가 있을 경우 `missingIngredients`로 별도 표시.
5. API 오류/형식 오류 시 재시도 버튼 제공.

## 비기능 요구사항
- 응답 대기 시간 목표: 15초 이내.
- 재료 목록이 비어있으면 API 호출 전에 클라이언트에서 막고 안내 메시지 표시.

## 성공 기준
- 1단계 재료 목록을 입력했을 때, 해당 재료를 실제로 활용하는 레시피가 최소 1개 이상 생성된다.
- 사용자가 레시피 상세 내용을 조리 단계까지 확인할 수 있다.

## 다음 단계 인터페이스
사용자가 선택한 `Recipe` 객체(및 원본 재료 목록)를 3단계 저장 기능으로 전달한다.

## 리스크 / 참고 사항
- `cohere/north-mini-code:free`는 이름상 코드 생성에 특화된 모델일 가능성이 있으나, 사전 테스트에서 일반 한국어 자연어 응답이 정상 동작함을 확인함. 레시피처럼 구조화된 JSON 출력 품질은 추가 검증이 필요하며, 품질이 부족하면 다른 텍스트 모델로 교체를 고려한다.
